# Pinned to the exact versions the submitted results were produced with
# (`pip freeze` of .venv on 21 Aug 2026). Python 3.14.6, CPU only.
#
# Direct dependencies only — every package below is imported somewhere in code/.
# Transitive pins are left to pip so the file stays readable; the versions that were
# actually resolved are listed at the bottom for exact reproduction.

lightgbm==4.7.0
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1
requests==2.34.2
requests-cache==1.3.3
scikit-learn==1.9.0

# Transitive versions as resolved in the reference environment, for byte-exact
# reproduction if ever needed:
#   attrs==26.1.0 cattrs==26.1.0 certifi==2026.7.22 charset-normalizer==3.5.1
#   colorama==0.4.6 contourpy==1.3.3 cycler==0.12.1 fonttools==4.63.0 idna==3.19
#   joblib==1.5.3 kiwisolver==1.5.0 narwhals==2.24.0 packaging==26.3 pillow==12.3.0
#   platformdirs==4.11.3 pyparsing==3.3.2 python-dateutil==2.9.0.post0 six==1.17.0
#   scipy==1.18.0 threadpoolctl==3.6.0 typing_extensions==4.16.0 tzdata==2026.3
#   url-normalize==3.0.0 urllib3==2.7.0
